
<p><strong>Review by:</strong> <%= user.first_name %> </p>
<p><%= review.description %></p>
<p><strong>Rating:</strong> <%= review.rating %></p>
<% if current_user == user %>
  <%= button_to 'Delete review', review, :class => 'btn', method: :delete, data: { confirm: 'Are you sure you want to delete this review?' } %>
<% end %>
